export default interface IArticle {
  date: string,
  excerpt: string,
  post_category_id: number,
  post_image: string,
  post_thumbnail: string,
  slug: string,
  title: string,
  deleted: boolean,
}